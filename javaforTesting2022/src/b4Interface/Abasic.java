package b4Interface;

public class Abasic {
	
	//Interface has keyword interface 
	//always define only abstract method
	///only method declaration
	//we can not create object of interface 
	//the variable are by default final i.e we need provide values which will not be changed 
	//we can't have constructor 
	//we have only one modifier i.e public 
	//100 % abstraction is possible, because we declare method.we not provide body
	//for INTERFACE, Implementation KEYWORD Is used.
	//no need to write Abstract Keyword in method by default method are Abstract

}
